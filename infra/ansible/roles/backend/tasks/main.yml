---
# Tasks for backend deployment
- name: Create backend directory
  file:
    path: /home/ig/backend
    state: directory

- name: Pull and run backend container
  docker_container:
    name: backend
    image: "{{ backend_image_url }}"
    state: started
    restart_policy: always
    ports:
      - "8080:8080"
